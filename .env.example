# Copy to .env and fill in. Do not commit .env (it may contain secrets).

# ----------- Dev Fix (Initiate dev fix / simulation) -----------
# Local: voiceaiagentv5 running on this machine
# PUBLIC_VOICE_AGENT_URL=http://localhost:8000
# Production: deployed voiceaiagentv5 API (no trailing slash)
# PUBLIC_VOICE_AGENT_URL=https://your-voice-agent-api.example.com

PUBLIC_VOICE_AGENT_URL=http://localhost:8000

# ----------- Supabase (required for app) -----------
# PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
